# A3S Science

The `a3s/science` cognitive package provides a read-only scientific research
Tool, an MCP server, a reusable Skill, and a research UI contribution.

All network requests remain explicit and return source metadata suitable for
review. See the package Skill and the repository's `DATA_SOURCES.md` for the
supported upstream services.
