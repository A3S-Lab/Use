---
title: 快速开始
description: 从源码构建 A3S Use 开发预览版、检查当前能力，并了解尚未满足的发布条件。
---

# 快速开始

A3S Use 是 A3S 的 **AI Native Package Manager**，用于解析和管理原生能力，
以及包含 Tool、MCP、OKF、A3S Flow、Skill、UI 的版本化认知包依赖图。

:::caution 开发预览
A3S Use 尚未发布受支持的产品版本，也未达到 production-ready。当前仅适合
从源码构建后进行开发和评估。仓库 tag、内部 schema 编号或单元测试通过都不
代表产品已经发布。
:::

## `main` 已实现什么

| 领域         | 当前实现                                                                                |
| ------------ | --------------------------------------------------------------------------------------- |
| 包格式       | Manifest v3、六类命名表面、必需的包 README                                              |
| 依赖         | 有界 SemVer 解析、精确 Registry/TUF lock、前向准备、一次切换、反向退休                  |
| 信任         | 可替换的宿主持有 Registry 与完整 catalog-v3 provenance                                  |
| 生命周期     | install、upgrade、uninstall、enable、disable 统一走不可变 plan-v4 审查/apply            |
| 本地表面宿主 | Tool Task、stdio MCP、Skill/UI projection、SQLite/FTS5 OKF、注入式 `a3s-flow` preflight |
| 托管宿主     | Runtime Service、HTTP MCP/Gateway、托管 Knowledge/UI 组合仍在开发                       |
| 平台         | Linux/macOS 开发门禁；Windows 已测签名包生命周期，但仍未达到发布级一致性                |

当前只接受一条预览协议基线：manifest v3、catalog v3、receipt v3、plan v4、
host protocol v4、manager tools v3、pending graph v2，以及 enablement
state/operation v2。过时的预览状态会被拒绝，并提示清理后重新安装。

## 安装 A3S CLI

A3S CLI 已提供独立于 Use 产品状态的跨平台安装器。macOS 与 glibc Linux
使用：

```bash
curl --proto '=https' --tlsv1.2 -LsSf \
  https://raw.githubusercontent.com/A3S-Lab/a3s/main/install.sh | sh
```

Windows PowerShell 5.1 或更高版本使用：

```powershell
irm https://raw.githubusercontent.com/A3S-Lab/a3s/main/install.ps1 | iex
```

脚本从官方 `A3S-Lab/CLI` 稳定 Release 选择当前平台归档、校验 SHA-256，
并以当前用户权限原子替换安装。默认不会改写 shell 配置或用户 `PATH`。

安装 CLI 后，可以显式安装并检查 CLI Release 携带的 Use 开发预览组件：

```bash
a3s install use --source release
a3s use doctor --json
```

:::warning 两种版本边界
上面的命令安装 CLI Release 固定的 Use 预览组件，不代表 Use 已发布受支持的
产品版本。要验证当前 `main` 的精确实现，仍应按下一节从源码构建。
:::

## 安装 standalone Use 预览版

standalone Use release installer 与上面的根 `a3s` CLI installer 相互独立。
先下载脚本以便审查；它要求 Cosign 按精确 tag workflow identity 与 GitHub
OIDC issuer 验证 release checksum，并只在签名、SHA-256 与提取检查通过后
发布命令。受管命令还会绑定归档内的 OCR model、OCR Skill 与 Browser Skill，
同时保留显式环境变量覆盖。

Linux 或 macOS：

```bash
curl --proto '=https' --tlsv1.2 -fsSLo /tmp/a3s-use-install.sh \
  https://raw.githubusercontent.com/A3S-Lab/Use/main/install.sh
sh /tmp/a3s-use-install.sh
```

Windows x86_64 与 PowerShell 7：

```powershell
$installer = Join-Path $env:TEMP 'a3s-use-install.ps1'
Invoke-WebRequest https://raw.githubusercontent.com/A3S-Lab/Use/main/install.ps1 -OutFile $installer
& $installer
```

需要先把 `cosign` 安装到 `PATH`，也可在 Unix 通过 `--cosign`、Windows
通过 `-CosignPath` 指定可信可执行文件。

这仍是 development-preview 分发路径。带 tag 的 release 已提供确定性归档
序列化、逐平台 SPDX SBOM、GitHub OIDC provenance/SBOM attestation 与
keyless Sigstore checksum bundle。installer 会在下载平台归档前 fail closed
地验证该 bundle，并把已验证 manifest 与 bundle 保留在安装版本目录中。
第二台无编译缓存的干净 runner 还必须逐字节匹配全部随包原生可执行文件，并
发布带 attestation 的 reproducibility 证据。剩余 bootstrap 边界与可选
GitHub attestation 验证见[信任与安全](./trust-security)。

## 从源码构建

需要 Rust 1.85 或更高版本：

```bash
git clone https://github.com/A3S-Lab/Use.git
cd Use
cargo build --workspace --bins --locked
./target/debug/a3s-use doctor --json
./target/debug/a3s-use capability snapshot --json
```

在 Use checkout 内运行仓库门禁：

```bash
cargo fmt --all -- --check
cargo test --workspace --all-targets --locked
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
```

## 试用开发版 CLI

standalone CLI 会先持久化由宿主选择的 Registry 信任边界，再从同一组 enabled
source 解析签名包及其完整 SemVer 依赖闭包：

```bash
a3s-use registry source add packages \
  --url https://packages.example.org/a3s/ \
  --trust-root sha256:<64-hex-digits> \
  --json

a3s-use install acme/research \
  --registry-name packages \
  --version 2.0.0 \
  --json

a3s-use upgrade acme/research \
  --registry-name packages \
  --json

a3s-use uninstall acme/research --json
```

应用单独审查过的解析结果时增加
`--package-lock-digest sha256:<64-hex-digits>`。lock 漂移会在下载 archive
前失败。source replace/default/enable/disable/remove 需要
`registry source list --json` 返回的精确 revision 与 `--yes`；identity-bound
TUF/cache state 和 installed provenance 会被保留。以上 Registry 值仅用于说明；
项目当前没有宣传公共生产 Registry。

对已经精确 promoted 的 OKF projection 执行带引用搜索：

```bash
a3s-use knowledge search "retrieval policy" --limit 5 --json
```

Runtime Service、HTTP MCP、Flow、Knowledge、Skill、UI 的 readiness 都依赖
embedding host 显式提供 owner。owner 缺失时直接 fail closed，不会替换为宽松
runner 或 source-only binding。

## standalone installer 仍缺少什么

预览 installer 与签名 release 证据已经存在；受支持的产品 installer 仍需在
外部运营的 witness 中复核完整 staged tree/最终归档、把证据保留在 GitHub
Release 之外、通过完整跨平台恢复矩阵，并完成剩余产品运维。CLI 携带的预览
组件不能替代这些门禁。

## 下一步

- [理解认知包模型](./package-model)
- [使用 A3S Flow 打包工作流](./flow)
- [设计 OKF 知识包](./okf)
- [配置 Registry 信任与授权](./trust-security)
- [查看 Use 与宿主之间的所有权](./architecture)
- [跟踪发布阻塞项](./roadmap)
